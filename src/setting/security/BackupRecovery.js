import React from 'react';
import {BackupRecovery} from 'tiklab-security-ui';

/**
 * 备份与恢复
 */
const BackupRecoveryContent = (props) => {
    return <BackupRecovery {...props} />
}

export default BackupRecoveryContent;
